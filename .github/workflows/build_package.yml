name: Build & Release

on:
  push:
    tags:
      - 'v*'

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - goos: linux
            goarch: 386
            artifact: web2api-linux-386
          - goos: linux
            goarch: amd64
            artifact: web2api-linux-amd64
          - goos: linux
            goarch: arm64
            artifact: web2api-linux-arm64
          - goos: linux
            goarch: arm
            artifact: web2api-linux-arm
          - goos: windows
            goarch: 386
            artifact: web2api-windows-386.exe
          - goos: windows
            goarch: amd64
            artifact: web2api-windows-amd64.exe
          - goos: windows
            goarch: arm64
            artifact: web2api-windows-arm64.exe
          - goos: windows
            goarch: arm
            artifact: web2api-windows-arm.exe
          - goos: darwin
            goarch: amd64
            artifact: web2api-darwin-amd64
          - goos: darwin
            goarch: arm64
            artifact: web2api-darwin-arm64

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: '1.22.6'

      - name: Build ${{ matrix.artifact }}
        env:
          GOOS: ${{ matrix.goos }}
          GOARCH: ${{ matrix.goarch }}
          CGO_ENABLED: 0
        run: go build -trimpath -ldflags="-s -w" -o "dist/${{ matrix.artifact }}" ./cmd/web2api

      - name: Upload release assets
        uses: softprops/action-gh-release@v2
        with:
          files: dist/${{ matrix.artifact }}

      - name: Cleanup old releases and tags
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          DRY_RUN: false
        run: |
          set -e

          KEEP=5
          REPO="${{ github.repository }}"

          echo "Fetching releases..."

          releases=$(gh release list -R "$REPO" \
            --limit 100 \
            --json tagName,createdAt \
            --jq 'sort_by(.createdAt) | reverse | .[].tagName')

          keep_tags=$(echo "$releases" | head -n $KEEP)

          echo "Tags to keep:"
          echo "$keep_tags"

          echo "Processing releases..."

          count=0
          for tag in $releases; do
            count=$((count+1))

            if [ $count -le $KEEP ]; then
              continue
            fi

            if [ "$DRY_RUN" = "true" ]; then
              echo "[DRY RUN] Would delete release + tag: $tag"
            else
              echo "Deleting release: $tag"
              gh release delete "$tag" -R "$REPO" -y || true
              git push origin ":refs/tags/$tag" || true
            fi
          done

          echo "Cleaning orphan tags..."

          all_tags=$(git ls-remote --tags origin | grep -v '\^{}' | awk '{print $2}' | sed 's|refs/tags/||')

          for tag in $all_tags; do
            if ! echo "$keep_tags" | grep -qx "$tag"; then
              if [ "$DRY_RUN" = "true" ]; then
                echo "[DRY RUN] Would delete orphan tag: $tag"
              else
                echo "Deleting orphan tag: $tag"
                git push origin ":refs/tags/$tag" || true
              fi
            fi
          done
